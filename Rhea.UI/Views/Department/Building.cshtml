@model Dictionary<string, int>

@{
    ViewBag.Title = "部门楼宇";
    Layout = null;
}

<div class="row-fluid">
    <div class="box span12">
        <div class="box-header well" data-original-title>
            <h2>
                <i class="icon-th-list"></i>楼层平面图</h2>
        </div>

        <div class="box-content">  
            <div class="row-fluid">  
                <div class="span10">
                    <div id="svg" style="visibility:hidden"></div>
                </div>                
                <div class="span2">
                    <p>
                        <a id="zoom-in" href="#" class="btn"><i class="icon-zoom-in"></i>放大</a>
                        <a id="zoom-out" href="#" class="btn"><i class=" icon-zoom-out"></i>缩小</a>
                    </p>
                    <div id="room-summary"></div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row-fluid">
    <div class="span6">
        @Html.Action("Details", new { controller = "Building", area = "Estate", id = Model["BuildingId"] })
    </div>
    <div class="span6">
        @Html.Action("FloorListByDepartment", new { controller = "Building", area = "Estate", id = Model["BuildingId"], departmentId = Model["DepartmentId"] })
    </div>
</div>

<script type="text/javascript">
    $(function () {
        var svg;
        var zoomRate = 1.1;

        $('a#zoom-in').click(function () {
            zoom2('zoomIn');
            return false;
        });

        $('a#zoom-out').click(function () {
            zoom2('zoomOut');
            return false;
        });

        function zoom2(zoomType) {          
            var width = parseFloat(svg.attr('width'));
            var height = parseFloat(svg.attr('height'));        

            if (zoomType == 'zoomIn') {
               
                width *= zoomRate;
                height *= zoomRate;
            }
            else if (zoomType == 'zoomOut') {
             
                width /= zoomRate;
                height /= zoomRate;
            }
            svg.attr('width', width);
            svg.attr('height', height);
        }

        $('a.floor-details').click(function () {
            var svgFile = '/Attachment/svg/300173.svg';

            if (svgFile != '') {
                $('div#svg').load(svgFile, function () {
                    $(this).css("visibility", "visible");
                    svg = $(this).children('svg');                         

                    $("svg g[id^='g']").click(function () {
                        var id = $(this).attr('id').toString().substring(1);
                        $("svg rect[class='room-select']").attr('class', 'fil0');
                        $(this).children('[ref=bound]').attr('class', 'room-select');
                        $('#room-summary').load('/Estate/Room/Summary', { id: id });
                    });
                });
            }

            return false;
        });
    });
</script>
