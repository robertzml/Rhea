@{
    ViewBag.Title = "生均教学行政用房";
    Layout = null;
}

@Html.Action("Heading", new { controller = "Common", area = "", title = ViewBag.Title, active = "房产统计" })

<div class="row-fluid">
    <div class="span12">

        <div class="box chart gradient">
            <div class="title">
                <h4>
                    <span class="icon16 icomoon-icon-bars"></span><span>历年生均教学行政用房图</span>
                </h4>
                <a href="#" class="minimize">Minimize</a>
            </div>

            <div class="content">
                <div class="row-fluid">
                    <div class="span12">
                        <div id="container-student-avg-educationarea" style="width: 100%; height: 650px;">
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>

<script type="text/javascript">
    $(function () {
        var colors = Highcharts.getOptions().colors;

        setColumnChart(0);

        function setColumnChart(data) {
            var option = {
                chart: {
                    renderTo: 'container-student-avg-educationarea',
                    type: 'column'
                },
                credits: {
                    enabled: false
                },
                title: {
                    text: '历年生均教学行政用房图'
                },
                xAxis: {
                    categories: [2013, 2014, 2015]
                },
                yAxis: {
                    min: 0,
                    title: {
                        text: '面积 (m2)'
                    }
                },
                tooltip: {
                    formatter: function () {
                        return this.y + ' m<sup>2</sup>';
                    }
                },
                plotOptions: {
                    column: {
                        pointPadding: 0.2,
                        borderWidth: 0
                    }
                },
                series: [{
                    name: "面积",
                    color: colors[5],
                    data: [78.5, 79.6, 0]
                }]
            };

            /*$.each(data, function (i, item) {
                option.series[0].data.push([item.Year, parseFloat(item.AverageArea)]);
                option.xAxis.categories.push(item.Year);
            });*/

            var chart1 = new Highcharts.Chart(option);
        }
    });
</script>