@{
    ViewBag.Title = "土地证";
    Layout = null;
}


@Html.Action("Heading", new { controller = "Common", area = "", title = ViewBag.Title, active = "房产概况" })

<div class="row-fluid">
    <div class="span12">
        <div class="box">
            <div class="title">
                <h4>
                    <span class="icon16 icomoon-icon-film-2"></span><span>@ViewBag.Title</span>
                </h4>
                <a href="#" class="minimize">Minimize</a>
            </div>

            <div class="content noPad">
                <img src="@ViewBag.LandCertificate" alt="土地证" />
            </div>
        </div>
    </div>
</div>