@model int
@{
    ViewBag.Title = "楼宇主页";
    Layout = null;
}

<div class="row-fluid">
    <div class="box span12">
        <div class="box-header well" data-original-title>
            <h2>
                <i class="icon-th-list"></i>楼层平面图</h2>
        </div>

        <div class="box-content">  
            <div class="row-fluid">  
                <div class="span10">
                    <div id="svg" style="visibility:hidden"></div>
                </div>                
                <div class="span2">
                    <p>
                        <a id="zoom-in" href="#" class="btn"><i class="icon-zoom-in"></i>放大</a>
                        <a id="zoom-out" href="#" class="btn"><i class=" icon-zoom-out"></i>缩小</a>
                    </p>
                    <div id="room-summary"></div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row-fluid">
    <div class="span6">
        @Html.Action("Details", new { controller = "Building", area = "Estate", id = Model })
    </div>
    <div class="span6">
        @Html.Action("FloorList", new { controller = "Building", area = "Estate", buildingId = Model })
    </div>
</div>

<script type="text/javascript">
    $(function () {
        var svg;
        var zoomRate = 1.1;

        $('a#zoom-in').click(function () {
            zoom2('zoomIn');
            return false;
        });

        $('a#zoom-out').click(function () {
            zoom2('zoomOut');
            return false;
        });

        function zoom2(zoomType) {
            //var viewBox = svg.attr('viewBox');
            //var viewBoxValues = viewBox.split(' ');
            var width = parseFloat(svg.attr('width'));
            var height = parseFloat(svg.attr('height'));
            //viewBoxValues[2] = parseFloat(viewBoxValues[2]);
            //viewBoxValues[3] = parseFloat(viewBoxValues[3]);

            if (zoomType == 'zoomIn') {
                //viewBoxValues[2] *= zoomRate; // Decrease the width and height attributes of the viewBox attribute to zoom in.
                //viewBoxValues[3] *= zoomRate;
                width *= zoomRate;
                height *= zoomRate;
            }
            else if (zoomType == 'zoomOut') {
                //viewBoxValues[2] /= zoomRate; // Increase the width and height attributes of the viewBox attribute to zoom out.
                //viewBoxValues[3] /= zoomRate;
                width /= zoomRate;
                height /= zoomRate;
            }
            //svg.attr('viewBox', viewBoxValues.join(' '));
            svg.attr('width', width);
            svg.attr('height', height);
        }

        $('a.floor-details').click(function () {
            var svgFile = '/Attachment/svg/300173.svg';

            if (svgFile != '') {
                $('div#svg').load(svgFile, function () {
                    $(this).css("visibility", "visible");
                    svg = $(this).children('svg');
                    var s0 = svg[0];
                    var viewBox = s0.getAttribute('viewBox');
                    //svg.attr('viewBox', viewBox);                   

                    $("svg g[id^='g']").click(function () {
                        var id = $(this).attr('id').toString().substring(1);
                        $("svg rect[class='room-select']").attr('class', 'fil0');
                        $(this).children('[ref=bound]').attr('class', 'room-select');
                        $('#room-summary').load('/Estate/Room/Summary', { id: id });
                    });
                });
            }

            return false;
        });
    });
</script>