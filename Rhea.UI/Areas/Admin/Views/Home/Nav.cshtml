@{
    ViewBag.Title = "Nav";
    Layout = null;
}

<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container-fluid">
            <button data-target=".nav-collapse" data-toggle="collapse" class="btn btn-navbar"
                type="button">
                <span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar">
                </span>
            </button>
            <a href="@Url.Action("Index", "Home")" class="brand" id="logo">房产管理系统</a>
            <div class="nav-collapse collapse">                
                <ul class="nav">
                    <li><a href="@Url.Action("Index", new { controller = "Home", area = "Estate" })">概况</a></li>
                    <li class="dropdown"><a id="drop1" class="dropdown-toggle" data-toggle="dropdown"
                        role="button" href="#">楼群</a></li>
                    <li class="dropdown"><a id="drop2" class="dropdown-toggle" data-toggle="dropdown"
                        role="button" href="#">部门</a></li>
                    <li><a href="@Url.Action("Index", new { controller = "Statistic", area = "" })">统计</a></li>
                    @if (Request.IsAuthenticated)
                    {                        
                        <li><a href="@Url.Action("Index", new { controller = "Home", area = "Admin" })">后台管理</a></li>
                    }
                    <li><a href="#">关于我们</a> </li>
                    <li><a href="@Url.Action("Logout", new { controller = "Account", area = "" })">注销</a> </li>
                </ul>
            </div>
            <!--/.nav-collapse -->
        </div>
    </div>
</div>
