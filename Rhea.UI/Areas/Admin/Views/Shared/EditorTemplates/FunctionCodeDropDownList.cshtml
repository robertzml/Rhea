@using Rhea.Business
@using Rhea.Data.Entities
@model string

<select id="FunctionCodeId" name="FunctionCodeId">
    <option value="">-- 请选择 --</option>
    @{
        IRoomService service = new MongoRoomService();
        List<RoomFunctionCode> codes = service.GetFunctionCodeList();

        foreach (var item in codes)
        {
        <option value="@item.CodeId" @(Model == item.CodeId ? "selected='selected'" : "")>
            @item.CodeId - @item.FunctionProperty
        </option>
        }
    }
</select>